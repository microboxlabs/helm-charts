# MIOT Modulith Chart

A Helm chart for deploying **MIOT Modulith** - the ModularIoT Quarkus modulith backend for fleet, driver, tracking and resource management.

## Overview

MIOT Modulith is a modular monolith built with Quarkus. Each component is independently toggleable at deploy time via feature flags:

- **Fleet** - vehicle monitoring, trips, routes and geofences
- **Driver** - driver profiles, assignments and activity tracking
- **Tracking** - GPS asset position ingestion via pluggable messaging (Pulsar or standalone)
- **Reactive + JDBC dual datasource** - Hibernate Reactive Panache for runtime queries, JDBC for Flyway migrations, both targeting the same PostgreSQL instance
- **Dual JWT authentication** - HS256 for M2M device endpoints, RS256 (JWKS) for web application endpoints, both from the same Auth0 tenant
- **Pluggable messaging** - log mode (standalone, no broker) or Pulsar for production
- **Prometheus metrics** and Quarkus health endpoints included out of the box

## Prerequisites

- Kubernetes 1.19+
- Helm 3.0+
- PostgreSQL database (external)
- Auth0 tenant with M2M API (HS256) and web API (RS256)
- Apache Pulsar (optional, only when `messaging.type=pulsar`)

## Installation

### Add the Helm repository

```bash
helm repo add mbl-charts https://microboxlabs.github.io/helm-charts
helm repo update
```

### Install with default configuration

```bash
helm install miot-modulith mbl-charts/miot-modulith
```

### Install with custom values

```bash
helm install miot-modulith mbl-charts/miot-modulith -f values.yaml
```

## Parameters

### Image Parameters

| Parameter | Description | Default |
|-----------|-------------|---------|
| `image.repository` | Container image repository | `ghcr.io/microboxlabs/miot-srv` |
| `image.tag` | Container image tag | `latest` |
| `image.pullPolicy` | Image pull policy | `IfNotPresent` |
| `imagePullSecrets` | Image pull secrets | `[]` |

### Deployment Parameters

| Parameter | Description | Default |
|-----------|-------------|---------|
| `replicaCount` | Number of pod replicas | `1` |
| `nameOverride` | Override chart name | `""` |
| `fullnameOverride` | Override full name | `""` |
| `podAnnotations` | Pod annotations | `{}` |
| `podLabels` | Pod labels | `{}` |
| `podSecurityContext` | Pod security context | `{}` |
| `securityContext` | Container security context | `{}` |
| `nodeSelector` | Node selector | `{}` |
| `tolerations` | Tolerations | `[]` |
| `affinity` | Affinity rules | `{}` |

### Service Parameters

| Parameter | Description | Default |
|-----------|-------------|---------|
| `service.type` | Kubernetes service type | `ClusterIP` |
| `service.port` | Service port | `8180` |

### Ingress Parameters

| Parameter | Description | Default |
|-----------|-------------|---------|
| `ingress.enabled` | Enable ingress | `false` |
| `ingress.className` | Ingress class name | `""` |
| `ingress.annotations` | Ingress annotations | `{}` |
| `ingress.hosts` | Ingress hosts configuration | See values.yaml |
| `ingress.tls` | TLS configuration | `[]` |

### APISIX Route Parameters

Alternative to standard Ingress — creates an [ApisixRoute](https://apisix.apache.org/docs/ingress-controller/references/apisix_route_v2/) CRD instead. Requires the APISIX Ingress Controller.

| Parameter | Description | Default |
|-----------|-------------|---------|
| `apisixRoute.enabled` | Enable ApisixRoute CRD (mutually exclusive with `ingress.enabled`) | `false` |
| `apisixRoute.ingressClassName` | IngressClass that links to the GatewayProxy | `apisix` |
| `apisixRoute.hosts` | List of hosts with paths | See values.yaml |
| `apisixRoute.hosts[].host` | Hostname to match | `api.modulariot.local` |
| `apisixRoute.hosts[].paths` | List of URI paths (supports `/*` wildcards) | `["/", "/*"]` |
| `apisixRoute.plugins` | List of APISIX plugins to attach to the route | `[]` |

### Component Parameters

Each flag maps to a `MIOT_COMPONENT_*_ENABLED` environment variable resolved by Quarkus MicroProfile Config.

| Parameter | Description | Default |
|-----------|-------------|---------|
| `components.fleet.enabled` | Enable fleet monitoring component | `true` |
| `components.driver.enabled` | Enable driver monitoring component | `false` |
| `components.tracking.enabled` | Enable GPS asset tracking component | `false` |

### Auth Parameters

Dual JWT authentication via `DualJwtAuthMechanism`. M2M device endpoints (annotated with `@M2MAuth`) are verified using HS256 with a shared secret. All other API endpoints are verified using RS256 via the Auth0 JWKS endpoint. See [ADR-003](https://modulariot.com/docs/reference/architecture-decisions/adr-003-dual-jwt-authentication) for details.

| Parameter | Description | Default |
|-----------|-------------|---------|
| `auth.hs256Issuer` | Auth0 issuer URL for M2M tokens (HS256) | `""` |
| `auth.rs256Issuer` | Auth0 issuer URL for web tokens (RS256) | `""` |
| `auth.hs256Secret` | HS256 signing secret (Auth0 API Signing Secret) | `""` |
| `auth.jwksUrl` | RS256 JWKS endpoint URL | `""` |
| `auth.hs256Audience` | Expected audience for M2M tokens | `""` |
| `auth.rs256Audience` | Expected audience for web tokens | `""` |
| `auth.existingSecret` | Use existing K8s secret for HS256 secret | `""` |
| `auth.existingSecretHs256Key` | Key inside existing secret | `hs256-secret` |

### Messaging Parameters

Controls how modules publish to external message brokers. In `log` mode, messages are logged at DEBUG level and not sent — no broker required. In `pulsar` mode, messages are published to Apache Pulsar.

| Parameter | Description | Default |
|-----------|-------------|---------|
| `messaging.type` | Messaging backend: `log` (standalone) or `pulsar` | `log` |
| `messaging.pulsarUrl` | Pulsar service URL (when `type=pulsar`) | `""` |
| `messaging.trackingTopic` | Pulsar topic for asset tracking events | `""` |

### Database Parameters

The modulith uses both a JDBC datasource (Flyway migrations) and a reactive datasource (Hibernate Reactive Panache) pointing at the same PostgreSQL instance. Both URLs are injected automatically from the values below.

| Parameter | Description | Default |
|-----------|-------------|---------|
| `postgresql.external.host` | PostgreSQL host | `postgresql` |
| `postgresql.external.port` | PostgreSQL port | `5432` |
| `postgresql.external.database` | Database name | `miot` |
| `postgresql.external.username` | Database username | `miot` |
| `postgresql.external.password` | Database password | `miot` |
| `postgresql.external.existingSecret` | Use existing secret for credentials | `""` |
| `postgresql.external.existingSecretUsernameKey` | Username key in existing secret | `username` |
| `postgresql.external.existingSecretPasswordKey` | Password key in existing secret | `password` |

### OIDC Parameters (Deprecated)

> **Deprecated since 0.3.0** — replaced by the `auth` section above. OIDC env vars are only rendered when `oidc.authServerUrl` is set (backwards compatibility). The Quarkus backend no longer includes the `quarkus-oidc` extension.

| Parameter | Description | Default |
|-----------|-------------|---------|
| `oidc.authServerUrl` | Keycloak realm URL | `""` |
| `oidc.clientId` | OIDC client ID | `miot-backend` |
| `oidc.clientSecret` | Client secret (chart-managed secret) | `""` |
| `oidc.existingSecret` | Use existing secret for client secret | `""` |
| `oidc.existingSecretKey` | Key inside existing secret | `client-secret` |

### Flyway Parameters

| Parameter | Description | Default |
|-----------|-------------|---------|
| `flyway.migrateAtStart` | Run database migrations on startup | `false` |

### Health Check Parameters

| Parameter | Description | Default |
|-----------|-------------|---------|
| `livenessProbe.httpGet.path` | Liveness probe path | `/q/health/live` |
| `livenessProbe.initialDelaySeconds` | Initial delay | `10` |
| `livenessProbe.periodSeconds` | Check period | `10` |
| `readinessProbe.httpGet.path` | Readiness probe path | `/q/health/ready` |
| `readinessProbe.initialDelaySeconds` | Initial delay | `5` |
| `readinessProbe.periodSeconds` | Check period | `10` |

### Resource Parameters

| Parameter | Description | Default |
|-----------|-------------|---------|
| `resources.limits.cpu` | CPU limit | `""` |
| `resources.limits.memory` | Memory limit | `""` |
| `resources.requests.cpu` | CPU request | `""` |
| `resources.requests.memory` | Memory request | `""` |

### Autoscaling Parameters

| Parameter | Description | Default |
|-----------|-------------|---------|
| `autoscaling.enabled` | Enable HPA | `false` |
| `autoscaling.minReplicas` | Minimum replicas | `1` |
| `autoscaling.maxReplicas` | Maximum replicas | `10` |
| `autoscaling.targetCPUUtilizationPercentage` | Target CPU utilization | `80` |

### Metrics Parameters

| Parameter | Description | Default |
|-----------|-------------|---------|
| `metrics.enabled` | Enable metrics | `false` |
| `metrics.serviceMonitor.enabled` | Enable ServiceMonitor | `false` |
| `metrics.serviceMonitor.interval` | Scrape interval | `30s` |
| `metrics.serviceMonitor.labels` | ServiceMonitor labels | `{}` |

## Example Configurations

### Basic Deployment (fleet only)

```yaml
replicaCount: 1

components:
  fleet:
    enabled: true

auth:
  rs256Issuer: "https://auth0.example.com/"
  jwksUrl: "https://auth0.example.com/.well-known/jwks.json"
  rs256Audience: "https://api.example.com/v1"

postgresql:
  external:
    host: "postgresql.database.svc.cluster.local"
    database: "miot"
    existingSecret: "miot-db-credentials"
    existingSecretUsernameKey: "username"
    existingSecretPasswordKey: "password"
```

### All Components with Auth0

```yaml
components:
  fleet:
    enabled: true
  driver:
    enabled: true
  tracking:
    enabled: true

auth:
  hs256Issuer: "https://dev-tenant.auth0.com/"
  rs256Issuer: "https://auth0.example.com/"
  hs256Secret: "your-api-signing-secret"
  jwksUrl: "https://auth0.example.com/.well-known/jwks.json"
  hs256Audience: "https://iot.example.com/v1/asset/track"
  rs256Audience: "https://api.example.com/v1"

messaging:
  type: "log"
```

### With Pulsar Messaging

```yaml
components:
  tracking:
    enabled: true

auth:
  hs256Issuer: "https://dev-tenant.auth0.com/"
  rs256Issuer: "https://auth0.example.com/"
  hs256Secret: "your-api-signing-secret"
  jwksUrl: "https://auth0.example.com/.well-known/jwks.json"
  hs256Audience: "https://iot.example.com/v1/asset/track"
  rs256Audience: "https://api.example.com/v1"

messaging:
  type: "pulsar"
  pulsarUrl: "pulsar://pulsar.messaging.svc.cluster.local:6650"
  trackingTopic: "persistent://streamhub/tracking/asset-positions"
```

### With Flyway Migrations at Start

```yaml
flyway:
  migrateAtStart: true

postgresql:
  external:
    host: "postgresql.database.svc.cluster.local"
    database: "miot"
    username: "miot"
    existingSecret: "miot-db-credentials"
    existingSecretPasswordKey: "password"
```

### With Ingress and TLS

```yaml
ingress:
  enabled: true
  className: nginx
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
  hosts:
    - host: api.modulariot.example.com
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: miot-modulith-tls
      hosts:
        - api.modulariot.example.com
```

### With APISIX Route

Uses an ApisixRoute CRD instead of a standard Ingress resource. Requires the [APISIX Ingress Controller](https://apisix.apache.org/docs/ingress-controller/getting-started/) with a GatewayProxy configured.

```yaml
ingress:
  enabled: false

apisixRoute:
  enabled: true
  ingressClassName: apisix
  hosts:
    - host: api.modulariot.example.com
      paths:
        - /api
        - /api/*
        - /q
        - /q/*
  plugins:
    - name: redirect
      enable: true
      config:
        http_to_https: true
    - name: cors
      enable: true
```

### Production (all components, autoscaling, existing secrets)

```yaml
replicaCount: 2

image:
  tag: "0.3.0"

components:
  fleet:
    enabled: true
  driver:
    enabled: true
  tracking:
    enabled: true

auth:
  hs256Issuer: "https://dev-tenant.auth0.com/"
  rs256Issuer: "https://auth0.example.com/"
  existingSecret: "miot-auth0-credentials"
  existingSecretHs256Key: "hs256-secret"
  jwksUrl: "https://auth0.example.com/.well-known/jwks.json"
  hs256Audience: "https://iot.example.com/v1/asset/track"
  rs256Audience: "https://api.example.com/v1"

messaging:
  type: "pulsar"
  pulsarUrl: "pulsar://pulsar.messaging.svc.cluster.local:6650"
  trackingTopic: "persistent://streamhub/tracking/asset-positions"

postgresql:
  external:
    host: "postgresql.database.svc.cluster.local"
    database: "miot"
    existingSecret: "miot-db-credentials"
    existingSecretUsernameKey: "username"
    existingSecretPasswordKey: "password"

flyway:
  migrateAtStart: true

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 8
  targetCPUUtilizationPercentage: 70

metrics:
  enabled: true
  serviceMonitor:
    enabled: true
    interval: 15s
    labels:
      release: prometheus

resources:
  requests:
    cpu: 200m
    memory: 256Mi
  limits:
    cpu: 1000m
    memory: 512Mi

ingress:
  enabled: true
  className: nginx
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
  hosts:
    - host: api.modulariot.example.com
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: miot-modulith-tls
      hosts:
        - api.modulariot.example.com
```

## Quarkus Endpoints

Once deployed, the following endpoints are available:

| Endpoint | Description |
|----------|-------------|
| `/q/health` | Health overview |
| `/q/health/live` | Liveness probe |
| `/q/health/ready` | Readiness probe |
| `/q/metrics` | Prometheus metrics |
| `/q/openapi` | OpenAPI specification |
| `/q/swagger-ui` | Swagger UI |

## Upgrading

### To 0.3.0

- **New:** `components.tracking` for GPS asset tracking module
- **New:** `auth` section replaces `oidc` for dual JWT authentication (HS256 + RS256)
- **New:** `messaging` section for pluggable external messaging (log/pulsar)
- **Deprecated:** `oidc` section — OIDC env vars only render when `oidc.authServerUrl` is set. Migrate to `auth` section.
- **Breaking:** The Quarkus backend no longer includes `quarkus-oidc`. Existing deployments using OIDC must switch to the `auth` section with Auth0 credentials.

### To 0.x

This is the initial release. No upgrade notes.

## Troubleshooting

### Pod fails to start with database connection error

1. Verify the PostgreSQL service is running and accessible
2. Check the database credentials in the secret
3. Ensure the database `miot` exists

```bash
kubectl logs -l app.kubernetes.io/name=miot-modulith
```

### Authentication errors (401)

1. Verify `auth.hs256Issuer` matches the `iss` claim in your M2M tokens and `auth.rs256Issuer` matches the `iss` claim in your web tokens
2. For M2M (HS256): check that `auth.hs256Secret` matches the Auth0 API Signing Secret
3. For web (RS256): verify `auth.jwksUrl` is reachable from the cluster
4. Check audience values match the Auth0 API identifiers

```bash
# Test from local machine
kubectl port-forward svc/<release-name>-miot-modulith 8180:8180

# Get an M2M token and test
TOKEN=$(curl -s --request POST --url https://auth0.example.com/oauth/token \
  --header 'content-type: application/json' \
  --data '{"client_id":"...","client_secret":"...","audience":"...","grant_type":"client_credentials"}' | jq -r '.access_token')

curl -H "Authorization: Bearer $TOKEN" http://localhost:8180/api/v1/asset/track
```

### Health checks failing

1. Check if Flyway migrations completed successfully (set `flyway.migrateAtStart: true` and review logs)
2. Verify database connectivity
3. Review application logs for startup errors

```bash
kubectl port-forward svc/<release-name>-miot-modulith 8180:8180
curl -s http://localhost:8180/q/health
```

### Metrics not appearing in Prometheus

1. Verify the ServiceMonitor is created: `kubectl get servicemonitor`
2. Check that the ServiceMonitor labels match your Prometheus selector
3. Verify the metrics endpoint is accessible: `curl <pod-ip>:8180/q/metrics`
