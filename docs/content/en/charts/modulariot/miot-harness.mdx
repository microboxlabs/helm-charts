# miot-harness Chart

A Helm chart for deploying **MIOT Harness** — the multi-agent FastAPI backend that powers ASK MIOT (`miot_harness/api/server.py`). Runs in its own cluster (separate from Coordinador) and reaches Coordinador via private connectivity (VNet peering, Private Link, PostgREST, or a `kubectl port-forward` sidecar for dev).

## Overview

The miot-harness chart deploys:

- **FastAPI process** on `:8000`, exposing `/health` and `/runs`.
- Read-only-rootfs container, runs as UID 65534.
- **Three credential channels** (Anthropic, OpenAI, Coordinador DSN) with `existingSecret`-first precedence and a chart-managed Secret fallback for dev.
- **Optional NetworkPolicy** (default off) — DNS, public HTTPS, Coordinador CIDR allowlist.
- **Optional tunnel sidecar** (default off) — `kubectl port-forward` inside the Pod for dev environments.
- **Optional ServiceMonitor** for Prometheus-Operator scraping of `/metrics`.
- **Optional PVC** for `run_store` survivability (defaults to `emptyDir`).

## Prerequisites

- Kubernetes 1.19+
- Helm 3.0+
- (Optional) Prometheus Operator if `metrics.serviceMonitor.enabled=true`.
- (Optional) A CSI driver / storage class if `persistence.enabled=true`.
- (Optional) A CNI that enforces `NetworkPolicy` if `networkPolicy.enabled=true` (Calico, Cilium, etc.).

## Installation

### Standalone

```bash
helm install harness microboxlabs/miot-harness \
  --namespace miot-harness \
  --create-namespace
```

### As part of the modulariot umbrella

```bash
helm install platform microboxlabs/modulariot \
  --namespace modulariot \
  --set miot-harness.enabled=true
```

The umbrella defaults `miot-harness.enabled: false` so existing platform installs don't suddenly get an agent runtime — opt in per environment.

## Parameters

### Image

| Parameter | Description | Default |
|-----------|-------------|---------|
| `image.repository` | Container image | `ghcr.io/microboxlabs/miot-harness` |
| `image.tag` | Image tag | Chart appVersion |
| `image.pullPolicy` | Pull policy | `IfNotPresent` |
| `imagePullSecrets` | Pull secrets | `[]` |

### Deployment

| Parameter | Description | Default |
|-----------|-------------|---------|
| `replicaCount` | Number of replicas | `1` |
| `nameOverride` | Override chart name | `""` |
| `fullnameOverride` | Override full name | `""` |
| `podAnnotations` | Pod annotations | `{}` |
| `podLabels` | Pod labels | `{}` |
| `podSecurityContext` | Pod-level security context | `{}` |
| `nodeSelector` | Node selector | `{}` |
| `tolerations` | Tolerations | `[]` |
| `affinity` | Affinity | `{}` |

### ServiceAccount

| Parameter | Description | Default |
|-----------|-------------|---------|
| `serviceAccount.create` | Create a ServiceAccount | `true` |
| `serviceAccount.automount` | Automount API credentials | `true` |
| `serviceAccount.annotations` | SA annotations (e.g. Workload Identity) | `{}` |
| `serviceAccount.name` | SA name (autogenerated if empty and `create=true`) | `""` |

### Volumes (free-form)

| Parameter | Description | Default |
|-----------|-------------|---------|
| `volumes` | Extra volumes (appended to the chart's required workspace volume) | `[]` |
| `volumeMounts` | Extra volumeMounts on the app container | `[]` |

### Service

| Parameter | Description | Default |
|-----------|-------------|---------|
| `service.type` | Service type | `ClusterIP` |
| `service.port` | Service port (matches `EXPOSE 8000` in Dockerfile) | `8000` |

### Ingress

| Parameter | Description | Default |
|-----------|-------------|---------|
| `ingress.enabled` | Enable Ingress | `false` |
| `ingress.className` | Ingress class | `""` |
| `ingress.annotations` | Ingress annotations | `{}` |
| `ingress.hosts` | Ingress hosts list | `chart-example.local` |
| `ingress.tls` | TLS config | `[]` |
| `httpRoute.enabled` | Use Gateway API HTTPRoute instead | `false` |

### Container security context

Defaults match the harness Dockerfile (UID 65534, read-only rootfs). Aligned with Pod Security Standards "restricted".

| Parameter | Description | Default |
|-----------|-------------|---------|
| `securityContext.runAsNonRoot` | | `true` |
| `securityContext.runAsUser` | | `65534` |
| `securityContext.runAsGroup` | | `65534` |
| `securityContext.readOnlyRootFilesystem` | | `true` |
| `securityContext.allowPrivilegeEscalation` | | `false` |
| `securityContext.capabilities.drop` | | `[ALL]` |

### Probes

Liveness uses `/health` (always 200 once the process is up). Readiness uses `/health/ready` (the platform may treat the Pod not-ready until Nexo is enabled, depending on plan-13 phase 5 wiring).

| Parameter | Description | Default |
|-----------|-------------|---------|
| `livenessProbe.httpGet.path` | | `/health` |
| `livenessProbe.httpGet.port` | | `http` |
| `livenessProbe.initialDelaySeconds` | | `5` |
| `livenessProbe.periodSeconds` | | `10` |
| `livenessProbe.timeoutSeconds` | | `3` |
| `livenessProbe.failureThreshold` | | `3` |
| `readinessProbe.httpGet.path` | | `/health/ready` |
| `readinessProbe.httpGet.port` | | `http` |
| `readinessProbe.initialDelaySeconds` | | `5` |
| `readinessProbe.periodSeconds` | | `10` |
| `readinessProbe.timeoutSeconds` | | `3` |
| `readinessProbe.failureThreshold` | | `3` |

### Resources / autoscaling

| Parameter | Description | Default |
|-----------|-------------|---------|
| `resources` | CPU / memory limits / requests | `{}` |
| `autoscaling.enabled` | Enable HPA | `false` |
| `autoscaling.minReplicas` | | `1` |
| `autoscaling.maxReplicas` | | `100` |
| `autoscaling.targetCPUUtilizationPercentage` | | `80` |

### Process / runtime

Each setting maps 1:1 to a `MIOT_HARNESS_*` env var read by `miot_harness/config.py`.

| Parameter | Description | Default | Env var |
|-----------|-------------|---------|---------|
| `env` | Environment label (`local` / `dev` / `staging` / `prod`) | `local` | `MIOT_HARNESS_ENV` |
| `defaultTenantId` | Default tenant context | `demo-tenant` | `MIOT_HARNESS_DEFAULT_TENANT_ID` |
| `defaultUserId` | Default user context | `demo-user` | `MIOT_HARNESS_DEFAULT_USER_ID` |
| `logLevel` | Log level (DEBUG / INFO / WARNING / ERROR / CRITICAL) | `INFO` | `MIOT_HARNESS_LOG_LEVEL` |
| `requestIdHeader` | HTTP header name for request ID propagation | `x-request-id` | `MIOT_HARNESS_REQUEST_ID_HEADER` |

### Nexo (Coordinador integration)

DSN precedence: `nexo.existingSecret` wins over `nexo.dsn`. Either way, the env var is projected via `secretKeyRef` — never as a literal `value:` (closes a leak vector that would otherwise put credentials into `kubectl describe pod` output).

| Parameter | Description | Default | Env var |
|-----------|-------------|---------|---------|
| `nexo.dsn` | Literal DSN (chart creates a Secret if non-empty) | `""` | `MIOT_HARNESS_NEXO_DSN` |
| `nexo.existingSecret` | Reference an out-of-band Secret holding the DSN | `""` | (same) |
| `nexo.existingSecretKey` | Field within that Secret | `dsn` | (same) |
| `nexo.dbAlias` | DB alias (logged in `pg_stat_activity`) | `coordinador-dev` | `MIOT_HARNESS_NEXO_DB_ALIAS` |
| `nexo.applicationName` | Postgres application_name | `miot-harness` | `MIOT_HARNESS_NEXO_APPLICATION_NAME` |
| `nexo.tenantLock` | Single-tenant pin | `mintral` | `MIOT_HARNESS_NEXO_TENANT_LOCK` |
| `nexo.searchPath` | Postgres search_path | `nexo` | `MIOT_HARNESS_NEXO_SEARCH_PATH` |
| `nexo.freshnessWarnMinutes` | Soft freshness gate | `30` | `MIOT_HARNESS_NEXO_FRESHNESS_WARN_MINUTES` |
| `nexo.freshnessRefuseMinutes` | Hard freshness gate (refuses to register tools) | `240` | `MIOT_HARNESS_NEXO_FRESHNESS_REFUSE_MINUTES` |
| `nexo.maxTurns` | Per-run turn cap | `8` | `MIOT_HARNESS_NEXO_MAX_TURNS` |
| `nexo.criticEnabled` | Enable the critic agent | `false` | `MIOT_HARNESS_NEXO_CRITIC_ENABLED` |
| `nexo.supervisorMode` | `rule` or `llm` | `rule` | `MIOT_HARNESS_NEXO_SUPERVISOR_MODE` |
| `nexo.models.filterExpert` | Model for filter-expert agent | `claude-haiku-4-5` | `MIOT_HARNESS_NEXO_FILTER_EXPERT_MODEL` |
| `nexo.models.analyst` | Model for analyst agent | `claude-sonnet-4-6` | `MIOT_HARNESS_NEXO_ANALYST_MODEL` |
| `nexo.models.synthesizer` | Model for synthesizer agent | `claude-sonnet-4-6` | `MIOT_HARNESS_NEXO_SYNTHESIZER_MODEL` |
| `nexo.models.critic` | Model for critic agent | `claude-sonnet-4-6` | `MIOT_HARNESS_NEXO_CRITIC_MODEL` |
| `nexo.models.summarizer` | Model for summarizer agent | `claude-haiku-4-5` | `MIOT_HARNESS_NEXO_SUMMARIZER_MODEL` |

### Provider API keys

Three modes per provider, in precedence order: `existingSecret` → `apiKey` → unset.

| Parameter | Description | Default |
|-----------|-------------|---------|
| `anthropic.existingSecret` | Reference an out-of-band Secret | `""` |
| `anthropic.existingSecretKey` | Field within that Secret | `api-key` |
| `anthropic.apiKey` | Literal value (chart creates a Secret) | `""` |
| `openai.existingSecret` | Reference an out-of-band Secret | `""` |
| `openai.existingSecretKey` | Field within that Secret | `api-key` |
| `openai.apiKey` | Literal value (chart creates a Secret) | `""` |

When at least one credential takes the `apiKey` path, the chart renders a single `<release>-credentials` Secret with the b64-encoded value(s). Defaults render no Secret.

### Connectivity — tunnel sidecar

For dev environments where Coordinador is reachable only via the cluster API. Default off; production should use VNet peering / Private Link / PostgREST instead. When enabled, set `nexo.dsn` (or the secret behind it) to point at `localhost:<tunnel.port>`.

| Parameter | Description | Default |
|-----------|-------------|---------|
| `connectivity.tunnel.enabled` | Run a 2nd container with `kubectl port-forward` | `false` |
| `connectivity.tunnel.image` | Sidecar image | `bitnami/kubectl:1.30` |
| `connectivity.tunnel.command` | Sidecar entrypoint | `["sh", "-c"]` |
| `connectivity.tunnel.args` | Sidecar args (default: port-forward loop) | (see values.yaml) |
| `connectivity.tunnel.port` | Local port the sidecar exposes | `6434` |
| `connectivity.tunnel.kubeconfigSecret` | Secret holding kubeconfig with port-forward rights | `""` |
| `connectivity.tunnel.kubeconfigSecretKey` | Field within that Secret | `kubeconfig` |
| `connectivity.tunnel.readinessProbe` | TCP-socket probe on local port (mandatory) | (see values.yaml) |
| `connectivity.tunnel.resources` | Sidecar resources | `{}` |

### NetworkPolicy

Standard `networking.k8s.io/v1` NetworkPolicy is L3/L4 only — pair with a CNI policy (Cilium FQDN, Calico GlobalNetworkPolicy) if you need hostname-based egress restriction.

| Parameter | Description | Default |
|-----------|-------------|---------|
| `networkPolicy.enabled` | Render the policy | `false` |
| `networkPolicy.dnsNamespace` | Namespace for DNS egress (mandatory) | `kube-system` |
| `networkPolicy.allowPublicHttps` | Egress to `0.0.0.0/0:443` excluding RFC1918 | `true` |
| `networkPolicy.coordinador.cidrs` | CIDRs for Coordinador access (must be set explicitly) | `[]` |
| `networkPolicy.coordinador.ports` | Ports allowed to Coordinador | `[6432/TCP, 5432/TCP]` |
| `networkPolicy.extraEgress` | Free-form `NetworkPolicyEgressRule` entries | `[]` |
| `networkPolicy.ingress.enabled` | Allow ingress from same-namespace Pods | `true` |

### Metrics / ServiceMonitor

The harness's `/metrics` endpoint is a forward-compatible target — the chart accepts the toggle now; the endpoint lands in plan 13 phase 5.

| Parameter | Description | Default |
|-----------|-------------|---------|
| `metrics.enabled` | Whether the harness exposes `/metrics` | `false` |
| `metrics.serviceMonitor.enabled` | Render the ServiceMonitor CR (requires Prometheus Operator) | `false` |
| `metrics.serviceMonitor.interval` | Scrape interval | `30s` |
| `metrics.serviceMonitor.scrapeTimeout` | Scrape timeout | `10s` |
| `metrics.serviceMonitor.labels` | Extra labels (e.g. `release: prometheus`) | `{}` |

### Persistence

The workspace volume at `/app/.miot-workspace` is **always** mounted (the read-only rootfs requires it). Default emptyDir survives container restarts but not pod rescheduling; opt in to PVC for `run_store` durability.

| Parameter | Description | Default |
|-----------|-------------|---------|
| `persistence.enabled` | Use a PVC instead of emptyDir | `false` |
| `persistence.existingClaim` | Reference an existing PVC (skips chart-created PVC) | `""` |
| `persistence.storageClass` | Storage class (cluster default if empty) | `""` |
| `persistence.accessModes` | Access modes | `[ReadWriteOnce]` |
| `persistence.size` | Storage request | `1Gi` |
| `persistence.annotations` | PVC annotations | `{}` |

## Example configurations

### Local dev — single-tenant, in-cluster sidecar

```yaml
env: "local"
nexo:
  dsn: "postgres://harness:devpw@127.0.0.1:6434/coordinador"
anthropic:
  apiKey: "sk-ant-dev-xxxxx"   # chart creates a Secret; do NOT commit values.yaml
connectivity:
  tunnel:
    enabled: true
    kubeconfigSecret: "harness-coordinador-kubeconfig"
    args:
      - |
        while true; do
          kubectl port-forward -n coordinador svc/pgbouncer 6434:6432 \
            --address 127.0.0.1 || true
          sleep 5
        done
```

### Staging — existingSecret everywhere, persistence + NetworkPolicy

```yaml
env: "staging"
replicaCount: 1

nexo:
  existingSecret: "harness-nexo-dsn"
anthropic:
  existingSecret: "harness-anthropic-key"
openai:
  existingSecret: "harness-openai-key"

persistence:
  enabled: true
  size: 5Gi
  storageClass: managed-csi

networkPolicy:
  enabled: true
  coordinador:
    cidrs:
      - 10.10.0.0/16
```

### Production — autoscaled, observability on, hardened

```yaml
env: "prod"
replicaCount: 3

resources:
  limits:
    cpu: 2000m
    memory: 2Gi
  requests:
    cpu: 500m
    memory: 1Gi

autoscaling:
  enabled: true
  minReplicas: 3
  maxReplicas: 10
  targetCPUUtilizationPercentage: 70

nexo:
  existingSecret: "harness-nexo-dsn"
  freshnessRefuseMinutes: 60   # tighter prod gate
anthropic:
  existingSecret: "harness-anthropic-key"

persistence:
  enabled: true
  size: 20Gi
  storageClass: premium-ssd

metrics:
  enabled: true
  serviceMonitor:
    enabled: true
    labels:
      release: prometheus

networkPolicy:
  enabled: true
  coordinador:
    cidrs:
      - 10.0.0.0/16
```

## Accessing the harness

```bash
# Health check (always 200 once the process is up)
kubectl port-forward -n miot-harness svc/harness 8000:8000
curl http://127.0.0.1:8000/health

# Readiness (reflects Nexo state in plan-13 phase 5+)
curl http://127.0.0.1:8000/health/ready
```

## Troubleshooting

### `/health` reports `nexo.enabled: false` despite `nexo.dsn` set

The harness's freshness gate (`miot-harness/integrations/nexo/boot.py`) refuses to register Nexo tools if the snapshot is older than `MIOT_HARNESS_NEXO_FRESHNESS_REFUSE_MINUTES` (default 240). Possible causes:

1. The Coordinador team hasn't refreshed the materialized views recently.
2. Wrong DSN — the harness reaches a stale read replica.
3. ACL — the `harness` PG role lacks read on the freshness probe.

Check `kubectl logs` for `Nexo: critical` entries; they enumerate the failure mode.

### Pod crashes on first `/runs` invocation with "read-only filesystem"

The container runs with `readOnlyRootFilesystem: true`. The chart mounts a workspace volume at `/app/.miot-workspace`; the harness writes `run_store` JSON there. If you've overridden `securityContext` or `volumeMounts` in a way that removes the workspace mount, the harness will crash on first write.

Verify with:

```bash
kubectl exec -n miot-harness <pod> -- ls -la /app/.miot-workspace
# should be writable by UID 65534
```

If the directory is missing or read-only, your override has dropped the chart's workspace volume. Re-check `volumes` and `volumeMounts` in your values.

### `networkPolicy.enabled=true` but Pods can't reach Anthropic / OpenAI

Two common causes:

1. **CNI doesn't enforce NetworkPolicy.** Some Flannel modes ignore policies entirely. Verify with `kubectl get pods -n kube-system | grep -E 'cilium|calico|antrea'`. If your cluster has no policy-enforcing CNI, the chart's NetworkPolicy is rendered but does nothing — Pods can reach everything regardless.
2. **Transparent egress proxy.** Some clusters intercept HTTPS via an egress proxy with its own allowlist. The chart's NetworkPolicy permits the destination IP, but the proxy denies the hostname. Audit the egress proxy config.

If the symptom is "Pods can reach Anthropic but not Coordinador", check `networkPolicy.coordinador.cidrs` — empty means no Coordinador rule is rendered (intentional fail-closed).

## Upgrading

```bash
helm upgrade harness microboxlabs/miot-harness \
  --namespace miot-harness \
  -f values.yaml
```

Rolling restarts: the chart's Deployment uses default `RollingUpdate` strategy. The harness's `/health/ready` is the readiness gate — pods join traffic only after Nexo bootstraps (or after the chart's startup grace, whichever applies).

## Uninstalling

```bash
helm uninstall harness -n miot-harness
```

Note: the chart-managed credentials Secret and the workspace PVC (if `persistence.enabled=true` and no `existingClaim`) are deleted. Back up `run_store` data first if it matters.
