# miot-app Chart

A Helm chart for deploying the main ModularIoT application - a Next.js application with authentication, maps, and IoT management capabilities.

## Overview

The miot-app chart deploys:
- Next.js application with server-side rendering
- NextAuth.js authentication (Google OAuth, Auth0, etc.)
- Map integrations (Mapbox, Google Maps)
- IoT device management dashboard

## Prerequisites

- Kubernetes 1.19+
- Helm 3.0+

## Installation

### Standalone

```bash
helm install miot-app microboxlabs/miot-app \
  --namespace modulariot \
  --create-namespace
```

### As part of umbrella chart

```bash
helm install modulariot microboxlabs/modulariot \
  --namespace modulariot \
  --set miot-app.enabled=true
```

## Parameters

### Image Parameters

| Parameter | Description | Default |
|-----------|-------------|---------|
| `image.repository` | Image repository | `ghcr.io/microboxlabs/miot-app` |
| `image.tag` | Image tag | Chart appVersion |
| `image.pullPolicy` | Pull policy | `IfNotPresent` |
| `imagePullSecrets` | Pull secrets | `[]` |

### Deployment Parameters

| Parameter | Description | Default |
|-----------|-------------|---------|
| `replicaCount` | Number of replicas | `1` |
| `podAnnotations` | Pod annotations | `{}` |
| `podLabels` | Pod labels | `{}` |
| `nodeSelector` | Node selector | `{}` |
| `tolerations` | Tolerations | `[]` |
| `affinity` | Affinity rules | `{}` |

### Service Parameters

| Parameter | Description | Default |
|-----------|-------------|---------|
| `service.type` | Service type | `ClusterIP` |
| `service.port` | Service port | `3000` |

### Ingress Parameters

| Parameter | Description | Default |
|-----------|-------------|---------|
| `ingress.enabled` | Enable ingress | `false` |
| `ingress.className` | Ingress class name | `""` |
| `ingress.annotations` | Ingress annotations | `{}` |
| `ingress.hosts[0].host` | Default hostname | `app.modulariot.local` |
| `ingress.tls` | TLS configuration | `[]` |

### Authentication Parameters

| Parameter | Description | Default |
|-----------|-------------|---------|
| `auth.secret` | Auth secret for JWT signing | `""` |
| `auth.existingSecret` | Use existing secret for auth secret | `""` |
| `auth.existingSecretKey` | Key in existing secret | `auth-secret` |
| `auth.authProviders` | Login buttons shown in UI (comma-separated, `*` = primary) | `microsoft-entra-id*,credentials` |
| `auth.providers.google.enabled` | Enable Google OAuth | `false` |
| `auth.providers.google.clientId` | Google client ID | `""` |
| `auth.providers.google.clientSecret` | Google client secret | `""` |
| `auth.providers.auth0.enabled` | Enable Auth0 (chart-managed secret) | `false` |
| `auth.providers.auth0.clientId` | Auth0 client ID | `""` |
| `auth.providers.auth0.clientSecret` | Auth0 client secret | `""` |
| `auth.providers.auth0.issuer` | Auth0 issuer URL | `""` |
| `auth.providers.auth0.audience` | Auth0 audience URL | `""` |
| `auth.providers.auth0.existingSecret` | Use existing secret for Auth0 credentials | `""` |
| `auth.providers.auth0.existingSecretClientIdKey` | Key for client ID in existing secret | `auth0-client-id` |
| `auth.providers.auth0.existingSecretClientSecretKey` | Key for client secret in existing secret | `auth0-client-secret` |

### Resource Parameters

| Parameter | Description | Default |
|-----------|-------------|---------|
| `resources.limits.cpu` | CPU limit | Not set |
| `resources.limits.memory` | Memory limit | Not set |
| `resources.requests.cpu` | CPU request | Not set |
| `resources.requests.memory` | Memory request | Not set |

### Autoscaling Parameters

| Parameter | Description | Default |
|-----------|-------------|---------|
| `autoscaling.enabled` | Enable HPA | `false` |
| `autoscaling.minReplicas` | Minimum replicas | `1` |
| `autoscaling.maxReplicas` | Maximum replicas | `10` |
| `autoscaling.targetCPUUtilizationPercentage` | Target CPU | `80` |

## Authentication Setup

### Generate Auth Secret

```bash
openssl rand -base64 32
```

### Using Values (Google OAuth)

```yaml
auth:
  secret: "your-generated-secret"
  providers:
    google:
      enabled: true
      clientId: "your-client-id.apps.googleusercontent.com"
      clientSecret: "your-client-secret"
```

### Using Values (Auth0)

Auth0 acts as an identity broker for social logins (Google, GitHub, etc.). Configure the social connections in your Auth0 dashboard, then provide the Auth0 credentials here:

```yaml
auth:
  secret: "your-generated-secret"
  authProviders: "google,github,credentials"
  providers:
    auth0:
      enabled: true
      clientId: "your-auth0-client-id"
      clientSecret: "your-auth0-client-secret"
      issuer: "https://auth0.microboxlabs.com"
      audience: "https://users.microboxlabs.com/v1"
```

### Using Existing Secret (Google)

Create the secret:

```bash
kubectl create secret generic miot-auth \
  --from-literal=auth-secret="your-secret" \
  --from-literal=google-client-id="your-id" \
  --from-literal=google-client-secret="your-secret" \
  -n modulariot
```

Reference it:

```yaml
auth:
  existingSecret: miot-auth
  providers:
    google:
      enabled: true
```

### Using Existing Secret (Auth0)

Create the secret:

```bash
kubectl create secret generic miot-auth0 \
  --from-literal=auth0-client-id="your-auth0-id" \
  --from-literal=auth0-client-secret="your-auth0-secret" \
  -n modulariot
```

Reference it:

```yaml
auth:
  authProviders: "google,github,credentials"
  providers:
    auth0:
      existingSecret: miot-auth0
      issuer: "https://auth0.microboxlabs.com"
      audience: "https://users.microboxlabs.com/v1"
```

You can also customize the secret key names if your secret uses different keys:

```yaml
auth:
  providers:
    auth0:
      existingSecret: my-custom-secret
      existingSecretClientIdKey: "my-id-key"
      existingSecretClientSecretKey: "my-secret-key"
      issuer: "https://auth0.microboxlabs.com"
```

## Example Configurations

### Development

```yaml
replicaCount: 1

image:
  tag: "latest"

auth:
  secret: "dev-secret-change-in-production"
```

### Production

```yaml
replicaCount: 3

image:
  tag: "v1.0.0"

resources:
  limits:
    cpu: 1000m
    memory: 1Gi
  requests:
    cpu: 250m
    memory: 512Mi

autoscaling:
  enabled: true
  minReplicas: 3
  maxReplicas: 10

ingress:
  enabled: true
  className: nginx
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
  hosts:
    - host: app.modulariot.com
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: app-tls
      hosts:
        - app.modulariot.com

auth:
  existingSecret: miot-auth-production
  providers:
    google:
      enabled: true

env:
  - name: NEXTAUTH_URL
    value: "https://app.modulariot.com"
```
