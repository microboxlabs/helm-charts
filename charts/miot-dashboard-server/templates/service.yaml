apiVersion: v1
kind: Service
metadata:
  name: {{ include "miot-dashboard-server.fullname" . }}
  labels:
    {{- include "miot-dashboard-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "miot-dashboard-server.selectorLabels" . | nindent 4 }}
