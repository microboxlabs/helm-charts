apiVersion: v1
kind: Service
metadata:
  name: {{ include "miot-docs.fullname" . }}
  labels:
    {{- include "miot-docs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "miot-docs.selectorLabels" . | nindent 4 }}
