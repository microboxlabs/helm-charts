apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "miot-docs.fullname" . }}-test-connection"
  labels:
    {{- include "miot-docs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "miot-docs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
