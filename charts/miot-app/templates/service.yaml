apiVersion: v1
kind: Service
metadata:
  name: {{ include "miot-app.fullname" . }}
  labels:
    {{- include "miot-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "miot-app.selectorLabels" . | nindent 4 }}
