apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "miot-app.fullname" . }}-test-connection"
  labels:
    {{- include "miot-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "miot-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
