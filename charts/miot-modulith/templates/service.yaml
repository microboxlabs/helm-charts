apiVersion: v1
kind: Service
metadata:
  name: {{ include "miot-modulith.fullname" . }}
  labels:
    {{- include "miot-modulith.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "miot-modulith.selectorLabels" . | nindent 4 }}
