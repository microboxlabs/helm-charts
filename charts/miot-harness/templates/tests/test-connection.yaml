apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "miot-harness.fullname" . }}-test-connection"
  labels:
    {{- include "miot-harness.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "miot-harness.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
