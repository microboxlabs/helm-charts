apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "miot-web-site.fullname" . }}-test-connection"
  labels:
    {{- include "miot-web-site.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "miot-web-site.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
