apiVersion: v1
kind: Service
metadata:
  name: {{ include "miot-calendar.fullname" . }}
  labels:
    {{- include "miot-calendar.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "miot-calendar.selectorLabels" . | nindent 4 }}
