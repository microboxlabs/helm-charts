apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-quarkus-sse.fullname" . }}
  labels:
    {{- include "helm-quarkus-sse.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-quarkus-sse.selectorLabels" . | nindent 4 }}
